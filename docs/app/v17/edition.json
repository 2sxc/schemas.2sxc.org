{
  "$id": "https://schemas.2sxc.org/app/v17/edition.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Edition",
  "type": "object",

  "properties": {
    "description": {
      "type": "string",
      "description": "The name is mainly for your own orientation that you know what it's for"
    },
    "deployTo": {
      "type": "string",
      "description": "The edition to deploy to. If not specified, the edition will not support auto-deploy."
    },
    "generateModelsEnabled": {
      "type": "boolean",
      "description": "Enable/Disable the generation of Models. This is necessary, so that the UI can warn users when content types change. Default is false."
    },
    "generateModelsConfiguration": {
      "type": "string",
      "description": "The configuration name of the model to generate. NOT IMPLEMENTED YET"
    }
  },
  "unevaluatedProperties": false
}